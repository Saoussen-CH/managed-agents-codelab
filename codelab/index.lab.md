---
id: managed-agents-gemini-api
summary: Build a full-stack AI agent that fetches tech news, writes editorial summaries, and generates a polished PDF — powered by a single call to the Managed Agents API on Gemini.
status: Draft
authors: Saoussen Chaabnia
categories: AI, Gemini API
tags: web
feedback link: https://github.com/googlecodelabs/feedback/issues/new?title=[managed-agents-gemini-api]
keywords: docType:Codelab, category:AI, product:GeminiAPI

---

# Build a Daily Tech Digest Agent with the Gemini Managed Agents API

## Overview

Duration: 05:00

In this codelab you will build **Daily Digest** — a full-stack web app powered by the Gemini Managed Agents API.

Type one click. Get back a complete daily tech newsletter — fetched from real websites, written in a sharp editorial voice, and exported as a polished PDF — all generated by a single autonomous AI agent running in a managed Linux sandbox.

### What you'll build

![Daily Digest App — Dashboard showing a live agent stream](diagrams/app-dashboard.png)

A React + FastAPI web app where:

- The agent fetches live headlines from Hacker News, TechCrunch, and The Verge
- Writes 2-3 sentence editorial summaries in your configured voice
- Generates a formatted PDF using a mounted `SKILL.md` file
- Streams every tool call, code execution, and text chunk to your browser in real time
- Supports multi-turn refinement in the same sandbox
- Can be saved as a named managed agent you invoke by ID

### The TODOs you'll implement

| TODO | File | Concept |
|---|---|---|
| 1 | `backend/services/agent_client.py` | Create the `genai.Client()` |
| 2 | `backend/services/agent_client.py` | First `interactions.create()` with `stream=True` |
| 3 | `backend/services/agent_client.py` | `system_instruction` + inline `environment.sources` |
| 4 | `backend/services/agent_client.py` | Extract `environment_id` + `interaction_id` from the stream |
| 5 | `backend/services/agent_client.py` | Multi-turn refinement with `previous_interaction_id` |
| 6 | `backend/routes/agents.py` | `agents.list()` and `agents.create()` |

### What you'll learn

- Make your first call to the **Managed Agents API** — one call, one Linux sandbox, an autonomous agent
- Stream agent events in real time using **Server-Sent Events (SSE)**
- Mount reusable files into the sandbox with **inline `environment.sources`**
- Use `AGENTS.md` for persistent behavioral rules and `SKILL.md` for discoverable skills
- Resume the same sandbox across turns with `previous_interaction_id` + `environment`
- Bake configuration into a saved **managed agent** with `agents.create()`

### What you'll need

- Python 3.10+ and [uv](https://docs.astral.sh/uv/getting-started/installation/)
- Node.js 18+
- A Gemini API key with billing — [aistudio.google.com/api-keys](https://aistudio.google.com/api-keys)
- ~$5 of API credit (each full run ≈ $0.50–$1.00)

---

## Set Up Your Environment

Duration: 05:00

### Clone the starter

```bash
git clone https://github.com/Saoussen-CH/managed-agents-codelab.git
cd managed-agents-codelab/codelab/starter
```

> aside positive
>
> **Already on your machine?** If you cloned the repo earlier, `cd` to `codelab/starter` — that is your working directory for all commands in this codelab.

### Configure your API key

```bash
cp .env.example .env
```

Open `.env` and paste your Gemini API key:

```text
GEMINI_API_KEY=your-key-here
```

> aside negative
>
> **Billing required.** The Managed Agents API charges for token usage. Each full digest run costs ~$0.50–$1.00. The free tier does not cover the Interactions API — add prepay credits at [aistudio.google.com/billing](https://aistudio.google.com/billing).

### Install dependencies

```bash
# Backend
uv sync

# Frontend
cd frontend && npm install && cd ..
```

### Run the app

Open two terminals:

**Terminal 1 — backend:**
```bash
uv run uvicorn backend.main:app --reload
```

Expected output:
```text
INFO:     Uvicorn running on http://127.0.0.1:8000 (Press CTRL+C to quit)
```

**Terminal 2 — frontend:**
```bash
cd frontend && npm run dev
```

Open http://localhost:5173. The app loads and all pages are accessible — but clicking **Run Digest** will fail with `TODO 1 not implemented`. That is expected.

> aside positive
>
> **The app is fully wired.** The FastAPI backend, SSE streaming, React frontend, PDF generator, and file storage are all pre-built. Your job is to fill in the Managed Agents API calls inside `backend/services/agent_client.py` and `backend/routes/agents.py`.

---

## Understand the Managed Agents API

Duration: 03:00

Before writing code, let's understand what makes the Managed Agents API different.

### The problem it solves

Building a real AI agent means writing a lot of infrastructure before you get to the interesting part:

- Provision a secure, isolated sandbox per user
- Wire up tool execution (web access, code runner, file system)
- Manage the reasoning loop — observe → plan → act → repeat
- Persist files across turns
- Scale it reliably

**The Managed Agents API removes that floor entirely.** One call provisions a Ubuntu Linux sandbox hosted by Google. The agent reasons, writes and runs code, fetches URLs, and manages files — autonomously — until the task is done. You describe the task. The agent does the work.

### Building blocks

| Concept | What it is |
|---|---|
| **`interactions.create()`** | The main API call — provisions a sandbox, runs the agent loop, returns the result |
| **`agent`** | The base agent to use. `"antigravity-preview-05-2026"` is the general-purpose Antigravity agent |
| **`environment`** | Configures the sandbox: `"remote"` for a fresh one, or a config object with sources and network rules |
| **`stream=True`** | Returns an iterable of events instead of waiting for the full result |
| **`system_instruction`** | The agent's persona for this call |
| **`environment.sources`** | Files mounted into the sandbox filesystem at startup |

### How it runs

```text
Your code calls interactions.create()
        │
        ▼
Google provisions Ubuntu sandbox (Python 3.12, Node 22, 4 CPU / 16 GB RAM)
        │
        ▼
Agent reasoning loop starts:
  plan → fetch URLs → run code → write files → reason → repeat
        │
        ▼
Events stream back in real time (tool calls, text, completions)
        │
        ▼
interaction.completed → returns environment_id + interaction_id
```

The sandbox persists for 7 days. You can resume it across turns using `environment_id`.

---

## TODO 1 — Create the Client

Duration: 05:00

Open the agent client file in your editor:

```bash
code backend/services/agent_client.py
```

Find the `_make_client()` function. You'll see:

```python
# TODO 1: Create the Gemini API client
# genai.Client() reads GEMINI_API_KEY from the environment automatically.
# Add the import and client creation inside _make_client() below:
#   from google import genai
#   log.info("Creating Gemini API client")
#   _client_singleton = genai.Client()
```

Replace the `# TODO 1:` comment block with:

```python
from google import genai
log.info("Creating Gemini API client")
_client_singleton = genai.Client()
```

### Why a singleton?

`genai.Client()` authenticates and sets up the HTTP connection. Creating it once and reusing it avoids re-authenticating on every request. The `threading.Lock` ensures only one thread creates it when multiple requests arrive simultaneously.

### Verify

Restart the backend (`Ctrl+C`, then `uv run uvicorn backend.main:app --reload`). You should see:

```text
INFO  digest  Daily Digest API started
```

No more `RuntimeError: TODO 1 not implemented`. The app can now connect to the Gemini API.

---

## TODO 2 — First Streaming Call

Duration: 08:00

Find the `_stream_sync()` function. Look for the `# TODO 2` comment:

```python
# TODO 2: Build the kwargs dict for interactions.create()
# Required fields: agent, input, stream=True
# stream=True returns an iterable of events instead of waiting for the full result.
#
# kwargs: dict = {
#     "agent": agent_id or BASE_AGENT,
#     "input": prompt,
#     "stream": True,
# }
kwargs: dict = {}
```

Replace the empty `kwargs: dict = {}` with:

```python
kwargs: dict = {
    "agent": agent_id or BASE_AGENT,
    "input": prompt,
    "stream": True,
}
```

### What `stream=True` gives you

Without streaming: your app waits 30–90 seconds with nothing happening, then gets the full result at once.

With `stream=True`: you receive an iterable of events as the agent works. Each event carries a piece of the work in progress:

| Event type | What it means |
|---|---|
| `interaction.created` | Sandbox provisioned, agent loop started |
| `step.start` (function_call) | Agent is about to call a tool |
| `step.delta` (text) | Agent is writing a text chunk |
| `step.delta` (function_result) | Tool returned a result |
| `interaction.completed` | Done — contains `environment_id` and `id` |

These events are what you see in the browser's stream feed — the `🔧 run_command`, `🌐 Fetching…`, and `✍️ Writing response…` labels.

### Verify

Click **Run Digest** in the browser. You should see `⚡ Interaction started` in the stream feed, then a `TODO 3 not implemented` error — expected. The first call is wired.

---

## TODO 3 — Add Environment and Voice

Duration: 10:00

Find the `# TODO 3` comment directly below TODO 2:

```python
# TODO 3: Add system_instruction and environment to kwargs
# ...
```

Add the following after your TODO 2 code:

```python
if agent_id:
    # Saved agent: "remote" forks a clean sandbox from the agent's base_environment.
    # AGENTS.md and SKILL.md are already baked in — no inline config needed.
    kwargs["environment"] = "remote"
else:
    # Inline config: mount AGENTS.md and SKILL.md directly into the sandbox.
    kwargs["system_instruction"] = config["voice"]
    kwargs["environment"] = {
        "type": "remote",
        "sources": _inline_sources(config["agents_md"], config["skill_md"]),
    }
```

### What `system_instruction` does

`system_instruction` sets the agent's persona for this call. It is additive with `AGENTS.md` — both apply when present.

The app's default voice (`config["voice"]`) reads:
```text
You are the editor of a sharp, slightly skeptical tech newsletter.
Your job is to inform, not just entertain.
For each story: explain what happened, why it matters, one pointed observation where earned.
```

### What `environment.sources` does

`_inline_sources()` builds two inline source entries that get written to the sandbox filesystem at startup:

| Target path | Content | Purpose |
|---|---|---|
| `.agents/AGENTS.md` | `config["agents_md"]` | The harness loads this automatically as persistent rules |
| `.agents/skills/digest-pdf/SKILL.md` | `config["skill_md"]` | The agent discovers and follows these PDF generation instructions |

```python
def _inline_sources(agents_md: str, skill_md: str) -> list[dict]:
    return [
        {"type": "inline", "target": ".agents/AGENTS.md", "content": agents_md},
        {"type": "inline", "target": ".agents/skills/digest-pdf/SKILL.md", "content": skill_md},
    ]
```

> aside positive
>
> **How skill discovery works.** The Antigravity harness scans `.agents/skills/` at startup and registers any `SKILL.md` it finds. The skill's YAML frontmatter (`name`, `description`) tells the agent what it does. When the task prompt says "generate a PDF using the digest-pdf skill", the agent finds and follows the skill instructions.

> aside positive
>
> **Inline vs saved.** Inline sources are mounted fresh on every call — ideal for a config the user can edit. When you save an agent in TODO 6, the sources are baked into `base_environment` so they don't need to be passed at invocation time.

### Verify

Click **Run Digest** again. This time the agent should start working — you'll see tool calls like `🔧 run_command`, `🌐 Fetching…`, and code execution steps in the stream feed. The run may take 1–3 minutes.

---

## TODO 4 — Extract IDs from the Completed Event

Duration: 05:00

Find the `_handle_event()` function, inside the `elif event_type == "interaction.completed":` block:

```python
elif event_type == "interaction.completed":
    # TODO 4: Extract environment_id and id from the completed interaction.
    # ...
    pass
```

Replace `pass` with:

```python
interaction = getattr(event, "interaction", None)
if interaction:
    env_id = getattr(interaction, "environment_id", None)
    iid    = getattr(interaction, "id", None)
    usage  = getattr(interaction, "usage", None)
    if usage:
        log.info("Usage — input=%s output=%s total=%s",
            getattr(usage, "total_input_tokens", "?"),
            getattr(usage, "total_output_tokens", "?"),
            getattr(usage, "total_tokens", "?"))
```

### Why these IDs matter

`environment_id` is the address of the sandbox where the agent ran. The PDF `digest.pdf` is still there at `/workspace/digest.pdf`. The **Download PDF** button calls `GET /api/runs/{id}/pdf`, which uses this ID to download the snapshot from the Files API.

`interaction_id` is this conversation turn. It is required for multi-turn refinement in TODO 5 — without it, a follow-up instruction would start a fresh conversation with no context.

> aside positive
>
> **Why not `output_text`?** The `interaction.completed` event carries **empty outputs** by design — the API docs say "use the preceding ContentDelta events for the actual output." The text already accumulated in `text_parts` from `StepDelta(type="text")` events. This is correct behavior, not a bug.

### Verify

Run a full digest. After it completes, check `data/runs/<id>.json`:

```bash
cat data/runs/*.json | python3 -m json.tool | grep -E "environment_id|interaction_id"
```

Expected output:
```text
"environment_id": "env_CAE...",
"interaction_id": "ChB..."
```

The **Download PDF** button should now appear in the Run view.

---

## TODO 5 — Multi-Turn Refinement

Duration: 08:00

Find the `_refine_sync()` function. Look for the `# TODO 5` comment:

```python
# TODO 5: Implement multi-turn refinement
# ...
put({"type": "error", "message": "TODO 5 not implemented — add multi-turn call"})
put(None)
return
```

Replace the three placeholder lines with:

```python
stream = client.interactions.create(
    agent=agent_id or BASE_AGENT,
    input=message,
    environment=environment_id,
    previous_interaction_id=interaction_id,
    stream=True,
)
```

Then remove the `put({"type": "error"...})` and `put(None)` and `return` lines that follow.

### Two independent dimensions

The Interactions API tracks state on two separate axes:

| Parameter | What it preserves |
|---|---|
| `environment=environment_id` | Files, installed packages, `/workspace/digest.pdf` |
| `previous_interaction_id=interaction_id` | Conversation context, reasoning trace, tool history |

Pass both to continue exactly where the previous turn left off. The agent still has `digest.pdf` at `/workspace/digest.pdf` and remembers everything it did in turn 1.

You can mix and match independently:

```text
Same env + new conversation  →  omit previous_interaction_id
New env + same conversation  →  set environment="remote", keep previous_interaction_id
Both                         →  full multi-turn (what we implement here)
```

### Verify

Run a full digest. Once it completes, type a refinement in the **Refine** panel, for example:

```text
Make the Hacker News section twice as long. Add a one-sentence TL;DR at the very top.
```

The agent should respond using context from the first turn — no re-fetching the web.

---

## TODO 6 — Save and List Managed Agents

Duration: 08:00

Open the agents route file:

```bash
code backend/routes/agents.py
```

### TODO 6 — List agents

Find `list_agents()`:

```python
return []  # TODO 6: replace with agents.list() call
```

Replace it with:

```python
try:
    result = _make_client().agents.list()
    agents = result.agents or []
    return [
        {
            "id": a.id,
            "description": getattr(a, "description", ""),
            "base_agent": getattr(a, "base_agent", BASE_AGENT),
        }
        for a in agents
    ]
except Exception as exc:
    _reset_client()
    raise HTTPException(status_code=502, detail=str(exc))
```

### TODO 7 — Create (save) an agent

Find `create_agent()`:

```python
raise HTTPException(status_code=501, detail="TODO 7 not implemented")
```

Replace it with:

```python
agent = _make_client().agents.create(
    id=req.id,
    base_agent=BASE_AGENT,
    description=req.description,
    system_instruction=config.voice,
    tools=[
        {"type": "code_execution"},
        {"type": "google_search"},
        {"type": "url_context"},
    ],
    base_environment={
        "type": "remote",
        "sources": _inline_sources(config.agents_md, config.skill_md),
    },
)
log.info("Agent created — id=%s", getattr(agent, "id", req.id))
return {"id": getattr(agent, "id", req.id), "description": getattr(agent, "description", req.description)}
```

### What `agents.create()` does

`agents.create()` bakes your current configuration into a saved managed agent. `base_environment` sets the default sandbox that every future invocation forks from.

```text
Inline config (TODO 3):
  Every call passes AGENTS.md + SKILL.md as sources → sent with every request

Saved agent (TODO 6):
  Sources baked into base_environment → inherited on every invocation
  Invocation: just agent="my-digest" + environment="remote"
```

Once saved, invoking the agent needs no `sources`, no `system_instruction` — it's all baked in.

> aside positive
>
> **Tools default.** The docs say tools default to `code_execution`, `google_search`, and `url_context` if omitted. We pass them explicitly to make the configuration visible.

### Verify

Go to the **Agents** page. Enter `my-digest` as the ID and click Save. The agent should appear in the list.

Go back to Dashboard. Select `my-digest` from the agent dropdown and run a digest. Check the debug logs — you'll see no `system_instruction` or `sources` in the request payload. The saved agent handles both.

---

## What Happens End to End

Duration: 03:00

Now that all TODOs are complete, here is the full flow:

```text
1. Browser → POST /api/runs
2. FastAPI → asyncio.Task → _stream_sync() in thread executor
3. _stream_sync() → client.interactions.create(..., stream=True)
4. Google provisions Ubuntu sandbox (Python 3.12, 4 CPU, 16 GB RAM)
5. Agent: fetches homepages → parses headlines → writes digest → generates PDF
6. Each event → asyncio.Queue → SSE endpoint → browser StreamFeed
7. interaction.completed → env_id + interaction_id saved to data/runs/<id>.json
8. Browser: Download PDF → GET /api/runs/{id}/pdf
9. Backend → GET generativelanguage.googleapis.com/v1beta/files/environment-{id}:download
10. Backend: extracts digest.pdf from tar → streams to browser
```

The FastAPI backend acts as a bridge between the browser's SSE connection and the synchronous `google-genai` streaming API. The sync stream runs in `run_in_executor`, pushing events into an `asyncio.Queue` via `loop.call_soon_threadsafe`. The SSE endpoint drains the queue and forwards events to the browser.

---

## Stretch Goals

Duration: 10:00

### Try a different voice

Go to the **Voice** page and replace the editorial voice with something completely different:

```text
You are a dry, technical analyst. No opinions. No humor.
Every story gets: what changed, the technical mechanism, the concrete impact.
Numbers over adjectives. Skip anything without a measurable effect.
```

Run a digest and compare the tone.

### Add a new source

Go to the **Sources** page and add a fourth URL, for example `https://www.wired.com`. No code change needed — the prompt includes all configured sources automatically.

### Fork from a live environment

After a successful run, you can fork the live sandbox into a new saved agent — with all packages pre-installed:

```python
# Add this to a Python script in codelab/starter/
from google import genai
from dotenv import load_dotenv
load_dotenv()

client = genai.Client()

# Paste your environment_id from data/runs/<id>.json
ENV_ID = "env_CAE..."

agent = client.agents.create(
    id="digest-forked",
    base_agent="antigravity-preview-05-2026",
    system_instruction="You are a sharp tech newsletter editor.",
    base_environment=ENV_ID,   # forks the live sandbox with reportlab already installed
)
print(f"Forked agent: {agent.id}")
```

Every invocation of `digest-forked` starts with reportlab pre-installed — no `pip install` on the first run.

### Add a second skill

Open **Skills** and add a second `SKILL.md` alongside the PDF skill. Mount it in `_inline_sources()`:

```python
{"type": "inline",
 "target": ".agents/skills/markdown-report/SKILL.md",
 "content": "...your skill content..."},
```

The harness will discover and register both skills. Update the prompt to ask for whichever one you want.

---

## Clean Up

Duration: 02:00

Delete any saved managed agents you created:

```bash
uv run python3 - << 'EOF'
from google import genai
from dotenv import load_dotenv
load_dotenv()

client = genai.Client()
result = client.agents.list()
for a in result.agents or []:
    client.agents.delete(id=a.id)
    print(f"Deleted: {a.id}")
EOF
```

Delete local run data:

```bash
rm -rf data/runs/
```

> aside positive
>
> **API credits.** Unused Gemini API prepay credits expire after 12 months. You can monitor usage at [aistudio.google.com/billing](https://aistudio.google.com/billing).

---

## Summary

Congratulations! You built a production-quality managed agent application on the Gemini API.

### What you built

| Component | What it does |
|---|---|
| `genai.Client()` | Single authenticated entry point to the Managed Agents API |
| `interactions.create(stream=True)` | Provisions a sandbox, runs the agent loop, streams events |
| `system_instruction` | Sets the agent's editorial voice per call |
| `environment.sources` (inline) | Mounts AGENTS.md + SKILL.md into the sandbox filesystem |
| `interaction.completed` | Carries `environment_id` + `interaction_id` for multi-turn + file download |
| `previous_interaction_id` + `environment` | Resumes the same sandbox and conversation |
| `agents.create()` | Bakes configuration into a saved agent, invokable by ID |

### Key patterns you learned

1. **One call, one sandbox** — `interactions.create()` handles all infrastructure
2. **stream=True** — see the agent reason and act in real time
3. **Inline sources** — mount files into the sandbox without any deployment
4. **Skill discovery** — the harness auto-discovers `SKILL.md` files in `.agents/skills/`
5. **Two-dimensional state** — `environment` for files, `previous_interaction_id` for conversation
6. **Saved agents** — bake configuration into `base_environment` for reuse

### Next steps

- Read the [Managed Agents API documentation](https://ai.google.dev/gemini-api/docs/managed-agents-quickstart)
- Explore the [Antigravity Agent](https://ai.google.dev/gemini-api/docs/antigravity-agent) capabilities and pricing
- Try the [Building Custom Agents](https://ai.google.dev/gemini-api/docs/custom-agents) guide for network rules and Git/GCS sources
- Build a different agent: a data analyst, a code reviewer, or a research synthesizer
