# Daily Digest — Frontend

React + TypeScript + Vite frontend for the Daily Digest app.

## Run

```bash
npm install
npm run dev   # http://localhost:5173
```

Requires the backend running on port 8000. See root README.
