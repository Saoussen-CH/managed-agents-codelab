# --- Gemini API (default) ---
GEMINI_API_KEY=your-key-here

# --- Vertex AI Agent Platform (set USE_VERTEX=true to switch) ---
# USE_VERTEX=true
# GOOGLE_CLOUD_PROJECT=dauntless-motif-494919-m3
# GOOGLE_CLOUD_LOCATION=global
# GCS_BUCKET=managed-agents   # bucket for uploading SKILL.md
#
# NOTE: GEMINI_API_KEY is also required in Vertex mode for PDF downloads.
# Environment snapshots are stored in Gemini file storage regardless of surface.
